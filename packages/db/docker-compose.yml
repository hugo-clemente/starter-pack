name: starter-pack

services:
  postgres:
    image: postgres
    container_name: starter-pack-postgres
    environment:
      POSTGRES_DB: starter-pack
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"
    volumes:
      - starter-pack_postgres_data:/var/lib/postgresql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

volumes:
  starter-pack_postgres_data:
